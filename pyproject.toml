[project]
name = "websearch-deep-research"
version = "0.1.0"
description = "A small deep-research pipeline prototype with staged progress and evidence tracking."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
deep-research = "deep_research.cli:main"
deep-research-server = "deep_research.server:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
