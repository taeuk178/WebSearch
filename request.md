# Request

<!-- 각 항목을 채워주세요. 짧아도 됩니다. 비워두면 AI가 추론하게 되어 결과물과 본래 의도 사이의 추적성이 떨어집니다. -->

## 무엇을 만드나요?
<!-- 만들려는 결과물의 형태와 목적. 앱·웹·API·문서·기능 등 어떤 형태든 괜찮습니다. -->

### 답변

Mac mini M4 36GB를 상시 실행되는 개인용 웹 검색 AI 서버로 구성한다.
LLM은 `mlx-community/gemma-4-26b-a4b-it-4bit`를 MLX로 Mac mini에서
로컬 실행하고, Open WebUI에서 검색·대화·출처 확인 기능을 제공한다.

외부 지역의 MacBook에서는 Open WebUI 주소나 모델 API 포트를 직접 열지 않는다.
MacBook과 Mac mini를 Tailscale로 연결한 뒤 SSH 로컬 포트 포워딩을 생성하고,
MacBook 브라우저에서 `http://127.0.0.1:<로컬 포트>`로 Open WebUI를 사용한다.

v1의 범위는 실시간 웹 검색을 수행하고, 검색 결과를 모델 답변과 함께
사용자에게 노출하는 것까지다. 검색 결과 저장, 대화 데이터 저장,
임베딩·벡터 인덱스 및 문서 RAG는 v2에서 구현한다.

저장소의 결과물은 새로운 웹 UI를 직접 개발하는 코드가 아니라 다음을 포함한
재현 가능한 설치·운영 패키지다.

- Mac mini용 모델 서버와 Open WebUI 실행 설정
- macOS 자동 실행 및 장애 후 재시작 설정
- MacBook에서 SSH 터널을 여는 `connect-gemma` 명령
- 설치, 보안 설정, 사용, 업데이트 및 문제 해결 문서
- 비밀값을 제외한 환경 변수 예시와 상태 점검 명령
- Open WebUI 웹 검색 호환성 스파이크 결과와 채택한 검색 연동 방식

## 핵심 차별점
<!-- 유사한 것들과 이 결과물이 어떻게 다른지 한 문장으로. 이 항목이 비어 있으면 AI가 방향을 임의로 결정합니다. -->

### 답변

LLM 추론과 실시간 검색 처리는 Mac mini에서 수행하고 검색 결과와 모델 답변에는
클릭 가능한 출처를 제공하되, Open WebUI와 모델 API는 루프백에만 바인딩하여
Tailscale을 통한 인증된 SSH 터널이 열려 있을 때만 원격에서 사용할 수 있다.
v1에서는 검색·대화 콘텐츠를 지식 데이터로 축적하지 않는다.

## 누구를 위해 만드나요?
<!-- 사용자·독자·소비자가 누구인지, 어떤 맥락에서 쓰는지. -->

### 답변

Mac mini 한 대를 개인 AI 서버로 사용하는 단일 사용자 본인을 위한 서비스다.
MacBook을 들고 집, 사무실, 출장지 등 다른 네트워크에 있을 때 주로 사용한다.

- 최신 뉴스와 기술 정보를 검색하고 한국어로 요약한다.
- 한국어와 영어 웹 자료를 함께 찾아 비교한다.
- 답변에 사용한 원문 링크를 열어 근거를 확인한다.
- 일반 대화와 실시간 웹 검색이 필요한 질문을 구분해서 사용한다.

동시 사용자는 1명이며 팀 서비스나 불특정 다수 공개 서비스는 고려하지 않는다.

## 반드시 포함할 것
<!-- 없으면 이 결과물이라고 할 수 없는 것들. -->

### 답변

- `mlx-community/gemma-4-26b-a4b-it-4bit`를 Mac mini의 MLX 환경에서 실행한다.
- 모델 서버는 OpenAI 호환 API를 제공하고 Open WebUI가 이 API에 연결된다.
- 모델 서버와 Open WebUI는 각각 Mac mini의 `127.0.0.1`에만 바인딩한다.
- 모델 API 포트와 Open WebUI 포트는 LAN, tailnet 및 공인 인터넷에 직접 노출하지 않는다.
- Mac mini와 MacBook은 같은 Tailscale tailnet에 연결한다.
- SSH는 공인 인터넷에 노출하지 않고 Tailscale 경로로만 접속한다.
- SSH는 비밀번호 로그인을 사용하지 않고 공개키 인증을 기본으로 한다.
- SSH 접근은 지정된 MacBook과 사용자만 허용하도록 Tailscale 접근 정책을 문서화한다.
- Mac mini는 Tailscale GUI 앱과 macOS 기본 SSH 원격 로그인을 사용한다.
  Tailscale SSH 서버는 v1에서 사용하지 않는다.
- MacBook용 `connect-gemma` 명령은 SSH 로컬 포트 포워딩을 생성한다.
  기본 전달 경로는 `MacBook 127.0.0.1:3000`에서
  `Mac mini 127.0.0.1:3000`으로 한다.
- `connect-gemma`는 포트 포워딩 실패를 감지하고 연결 유지 옵션을 사용하며,
  사용자가 중단하면 터널과 로컬 접근 경로를 함께 종료한다.
- 선택 옵션 또는 별도 명령으로 SSH 터널 생성 후 기본 브라우저에서
  `http://127.0.0.1:3000`을 열 수 있게 한다.
- Open WebUI 자체 로그인도 활성화하여 SSH 인증과 애플리케이션 인증을 함께 사용한다.
- Open WebUI는 Python 3.11과 `uv`를 사용해 macOS 호스트에 네이티브 설치한다.
- Open WebUI와 `mlx-lm`은 연동 시험을 통과한 버전으로 고정하고,
  업데이트 전 일반 채팅·웹 검색·스트리밍·출처 표시 회귀 시험을 수행한다.
- Open WebUI의 계정과 실행 설정은 영구 경로에 보존하되,
  v1에서는 채팅을 임시 대화로 사용하여 대화 내용을 영구 저장하지 않는다.
- 자동완성, 후속 질문, 대화 제목 및 태그 생성을 비활성화하여
  사용자 요청 외의 추가 LLM 호출이 발생하지 않게 한다.
- Open WebUI에서 일반 채팅과 웹 검색 모드를 구분해 사용할 수 있다.
- 구현의 첫 단계에서 고정한 Open WebUI와 `mlx-lm` 버전을 대상으로
  Legacy 웹 검색과 다음 설정 조합의 호환성 스파이크를 수행한다.
  - `BYPASS_WEB_SEARCH_EMBEDDING_AND_RETRIEVAL=True`
  - `BYPASS_WEB_SEARCH_WEB_LOADER=True`
- 스파이크가 통과하면 모델의 네이티브 도구 호출, 임베딩, 벡터 검색 및
  전체 페이지 수집 없이 검색 엔진의 제목·URL·스니펫을 모델에 직접 전달한다.
- 스파이크가 실패하면 실패 조건과 로그를 기록하고,
  동일한 입출력을 제공하는 별도의 상태 없는 검색 연동 계층을 v1 대안으로 구현한다.
- 검색 공급자는 최대 5개의 결과를 반환하고 각 결과의 제목, URL 및 요약문을 노출한다.
- 검색 결과는 해당 요청을 처리하는 동안 메모리에서만 사용하고,
  파일·데이터베이스·임베딩 저장소에 영구 저장하지 않는다.
- 검색 답변에는 주장과 근거를 연결해 확인할 수 있는 클릭 가능한 출처를 표시한다.
- 검색 결과가 부족하거나 출처가 충돌하면 이를 명시하고 확정적인 사실처럼 답하지 않는다.
- 기본 답변 언어는 한국어로 하되 필요하면 영어 검색어로도 확장한다.
- 검색 공급자는 별도 API 키 없이 시작할 수 있는 DuckDuckGo를 기본값으로 사용하되,
  나중에 SearXNG, Brave 또는 Tavily로 교체할 수 있게 설정을 분리한다.
- FileVault는 유지하며 Mac mini 재부팅 후 한 번의 물리적 사용자 로그인을 허용한다.
- 사용자 로그인 후 LaunchAgent가 모델 서버와 Open WebUI를 자동 실행하고,
  프로세스가 비정상 종료되면 다시 시작된다.
- 모델을 최초 한 번 내려받은 뒤에는 실행할 때마다 다시 다운로드하지 않는다.
- 모델 컨텍스트와 출력 길이를 메모리 한도 안에서 제한할 수 있어야 한다.
  초기값은 최대 컨텍스트 16K 이하, 최대 출력 2K 이하로 두고 실제 측정 후 조정한다.
- 모델 로딩 실패, 검색 실패, SSH 연결 실패, 메모리 부족 및 서비스 재시작 원인을
  확인할 수 있는 로그와 상태 점검 방법을 제공한다.
- 비밀번호, SSH 개인키, API 키, Tailscale 인증 키 등 비밀값은 저장소에 커밋하지 않는다.

## 포함하지 않을 것
<!-- 지금 범위에서 명시적으로 배제하는 것. -->

### 답변

- Open WebUI를 대체하는 새로운 채팅 프런트엔드 개발
- Tailscale Serve, Tailscale Funnel 또는 Cloudflare 공개 터널
- 공유기 포트 포워딩이나 공인 IP를 통한 직접 접속
- Open WebUI나 MLX API를 tailnet 주소에 직접 바인딩하는 구성
- MacBook에서 모델을 직접 실행하는 구성
- 여러 사용자, 조직 또는 권한 그룹을 위한 계정 관리
- 유료 클라우드 LLM API나 외부 GPU 서버를 이용한 추론
- 모델 파인튜닝, 추가 양자화 또는 다른 형식으로의 모델 변환
- 이미지·음성·영상 입력과 생성
- 로컬 문서 업로드 및 문서 RAG
- 로그인된 개인 브라우저를 조작하는 자동화
- 임의 코드 실행, 셸 명령 실행 또는 자율 에이전트 기능
- 초기 버전에서 모델이 검색과 URL 방문을 반복 결정하는 네이티브 에이전트 검색
- 검색 결과, 페이지 본문 및 대화 내용의 영구 저장
- 임베딩 생성, 벡터 데이터베이스, 지식 베이스 및 저장된 데이터를 대상으로 한 RAG
- 웹 출처 내용의 정확성 자체를 보증하는 기능

## 제약 조건
<!-- 기술 스택, 플랫폼, 환경, 외부 의존성 등 구현에 영향을 주는 조건. 없으면 비워도 됩니다. -->

### 답변

- 서버는 Apple Silicon M4와 36GB 통합 메모리를 탑재한 Mac mini다.
- macOS 15 이상을 기준으로 한다.
- 모델은 `mlx-community/gemma-4-26b-a4b-it-4bit`로 고정한다.
- 모델 추론은 MLX를 사용해 macOS 호스트에서 실행한다.
- 사용자 인터페이스는 Open WebUI를 사용하며 Docker가 아닌 Python 3.11 `uv`
  네이티브 설치를 기준으로 한다.
- MacBook의 원격 접속은 Tailscale 위의 SSH 로컬 포트 포워딩으로 제한한다.
- SSH 서버는 공인 인터넷에 노출하지 않으며 공유기에서 22번 포트를 포워딩하지 않는다.
- SSH는 macOS 기본 원격 로그인과 공개키 인증을 사용하고 비밀번호 인증을 비활성화한다.
- 모델 서버는 한 번에 한 요청을 안정적으로 처리하는 단일 사용자 구성으로 시작한다.
- 웹 검색을 위해 검색어와 대상 웹 페이지 요청은 외부로 전송될 수 있지만,
  전체 대화와 모델 추론은 외부 LLM API로 전송하지 않는다.
- 웹 페이지는 신뢰할 수 없는 입력으로 취급하고 페이지 안의 명령문이나
  프롬프트를 시스템 지시로 실행하지 않는다.
- 주요 구성요소의 설치 버전을 기록하고 무조건 최신 태그만 따라가지 않는다.
- 검색·대화 콘텐츠를 영구 저장하지 않는다. Open WebUI 로그인 계정, 보안 설정,
  고정된 구성요소 버전 등 서비스 운영에 필요한 최소 설정 데이터만 저장할 수 있다.
- FileVault를 유지하므로 재부팅 후 최초 한 번은 Mac mini에서 물리적으로 로그인해야 한다.
- 메모리 부족 징후가 있으면 컨텍스트 길이와 검색 결과 수를 순서대로 줄인다.
- Mac mini가 켜져 있고 사용자 세션, 인터넷, Tailscale 및 SSH가 정상이어야
  외부 MacBook에서 사용할 수 있다.

## 완성 기준
<!-- "됐다"고 할 수 있는 시점. 검토자가 이 기준으로 명세를 평가합니다. -->

### 답변

- Mac mini에서 모델 서버와 Open WebUI가 정상 기동되고 모델 목록에
  `mlx-community/gemma-4-26b-a4b-it-4bit`가 표시된다.
- Mac mini에서 `lsof` 또는 동등한 명령으로 확인했을 때 Open WebUI와 MLX API가
  `127.0.0.1`에서만 수신하고 `0.0.0.0`이나 Tailscale IP에서 수신하지 않는다.
- MacBook을 휴대전화 핫스팟 등 Mac mini와 다른 네트워크에 연결한 상태에서
  Tailscale과 `connect-gemma`를 통해 SSH 터널을 열 수 있다.
- 터널이 열린 동안 MacBook 브라우저의 `http://127.0.0.1:3000`에서
  Open WebUI 로그인과 대화를 정상적으로 사용할 수 있다.
- SSH 터널을 종료하면 MacBook의 `http://127.0.0.1:3000` 접근도 즉시 실패한다.
- SSH 터널 없이 Mac mini의 LAN 주소, Tailscale 주소 또는 공인 주소로
  Open WebUI와 MLX API 포트에 직접 접근할 수 없다.
- MacBook에서 Tailscale을 끄면 SSH 터널을 새로 만들 수 없다.
- 비밀번호 SSH 로그인이 거부되고 등록된 키를 가진 허용 사용자만 접속할 수 있다.
- 최신 정보가 필요한 질문을 입력하면 실제 웹 검색이 수행되고,
  한국어 답변에 서로 다른 웹 문서의 클릭 가능한 출처가 3개 이상 표시된다.
- 검색 결과 목록에서 각 결과의 제목, URL 및 요약문을 확인할 수 있다.
- 검색 한 번에 결과가 5개를 초과하지 않는다.
- 호환성 스파이크가 통과한 경우 웹 로더가 전체 페이지를 가져오지 않고,
  임베딩 또는 벡터 검색 없이 검색 스니펫만 모델에 전달되는 것을 확인한다.
- 호환성 스파이크가 실패한 경우 별도의 상태 없는 검색 연동 계층이
  동일한 검색 결과·답변·출처 표시 기준을 충족한다.
- 검색하지 않은 일반 대화에서는 불필요한 웹 요청이 발생하지 않는다.
- 자동완성, 후속 질문, 대화 제목 및 태그 생성을 위한 추가 모델 요청이 발생하지 않는다.
- 특정 URL을 읽지 못하거나 검색 공급자가 실패해도 전체 서비스가 종료되지 않고
  사용자에게 검색 실패 사실을 알린다.
- 모델이 예열된 상태에서 웹 검색 질문 5개의 첫 토큰 응답 시간 중앙값을 측정하고,
  목표인 30초 이내 달성 여부와 실제 결과를 운영 문서에 기록한다.
- 웹 검색 질문을 10회 연속 실행해도 모델 서버가 메모리 부족으로 종료되지 않는다.
- 검색 요청이 종료된 뒤 검색 결과, 임베딩 또는 벡터 인덱스가
  파일이나 데이터베이스에 남지 않는 것을 확인한다.
- 임시 대화를 종료한 뒤 해당 대화 내용이 Open WebUI 대화 기록에 남지 않는다.
- Mac mini 재부팅 후 사용자가 한 번 물리적으로 로그인하면,
  각 서비스를 터미널에서 수동 실행하지 않아도
  모델 서버와 Open WebUI가 복구되고 새 SSH 터널을 통해 다시 접속할 수 있다.
- 로그와 네트워크 점검을 통해 프롬프트가 외부 LLM API로 전송되지 않는 것을 확인한다.
- 새 MacBook에서도 문서에 따라 Tailscale, SSH 키 및 `connect-gemma`를 설정하면
  동일한 방식으로 접속할 수 있다.
- 새 Mac mini 환경에서 문서 순서대로 따라 하면 모델 다운로드 시간을 제외하고
  설치와 기본 설정을 재현할 수 있다.
